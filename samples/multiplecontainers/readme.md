# Using docker-compose

#### Create docker file as below
[Dockerfile](docker-compose.yml)

#### Run docker-compose
```shell script
docker-compose up -d
```

#### Access the application
http://localhost:8000
